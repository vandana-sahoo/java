
if (ch == 'A') {
    // Logic for when the character is 'A'
}

if (ch == 'A' || ch == 'E' || ch == 'I' || ch == 'O' || ch == 'U' || 
    ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u') {
    // Logic for when the character is a vowel (upper or lowercase)
}

if ((ch >= 'a' && ch <= 'z' || ch >= 'A' && ch <= 'Z') && !(ch == 'A' || ch == 'E' || ch == 'I' || ch == 'O' || ch == 'U' || 
    ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u')) {
    // Logic for when the character is a consonant
}

if (ch >= 'a' && ch <= 'z' && !(ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u')) {
    // Logic for when the character is a lowercase consonant
}


if (ch >= 'A' && ch <= 'Z' && !(ch == 'A' || ch == 'E' || ch == 'I' || ch == 'O' || ch == 'U')) {
    // Logic for when the character is an uppercase consonant
}



if (ch == 'e') {
    // Logic for when the character is 'e'
}

if (ch >= 'A' && ch <= 'Z') {
    // Logic for when the character is uppercase
}


if (ch >= 'a' && ch <= 'z') {
    // Logic for when the character is lowercase
}


